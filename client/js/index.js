moment.lang($('html').attr('lang'));
module.exports = {
    base: require('./base'),
    forum: require('./forum'),
    organization: require('./organization'),
    projects: require('./projects')
};
